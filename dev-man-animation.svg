<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 200">
  <style>
    .bg { fill: #0a0a0f; }
    .maze-line { stroke: #00ff88; stroke-width: 2; fill: none; opacity: 0.3; }
    .maze-glow { stroke: #00ff88; stroke-width: 4; fill: none; opacity: 0.1; filter: blur(3px); }
    
    /* DEV-MAN (Pac-Man style) */
    .devman { fill: #ffff00; }
    .devman-eye { fill: #0a0a0f; }
    
    /* Enemies */
    .bug { fill: #ff3366; }
    .merge-conflict { fill: #ff8800; }
    .tech-debt { fill: #aa55ff; }
    .enemy-eye { fill: #ffffff; }
    .enemy-pupil { fill: #0a0a0f; }
    
    /* Collectibles */
    .dot { fill: #00ff88; }
    .power-dot { fill: #00d4ff; }
    .coffee { fill: #8B4513; }
    
    /* Text */
    .title { 
      font-family: 'Courier New', monospace; 
      font-size: 24px; 
      font-weight: bold;
      fill: #00ff88;
    }
    .subtitle {
      font-family: 'Courier New', monospace;
      font-size: 12px;
      fill: #888;
    }
    .score-text {
      font-family: 'Courier New', monospace;
      font-size: 14px;
      fill: #00d4ff;
    }
    
    /* Animations */
    @keyframes devman-move {
      0% { transform: translateX(0); }
      100% { transform: translateX(500px); }
    }
    
    @keyframes devman-chomp {
      0%, 100% { d: path("M 0,0 L 15,-12 A 15 15 0 1 1 15,12 Z"); }
      50% { d: path("M 0,0 L 15,0 A 15 15 0 1 1 15,0 Z"); }
    }
    
    @keyframes bug-chase {
      0% { transform: translateX(0); }
      100% { transform: translateX(500px); }
    }
    
    @keyframes merge-chase {
      0% { transform: translateX(0); }
      100% { transform: translateX(500px); }
    }
    
    @keyframes debt-chase {
      0% { transform: translateX(0); }
      100% { transform: translateX(500px); }
    }
    
    @keyframes dot-eaten {
      0%, 80% { opacity: 1; r: 4; }
      90%, 100% { opacity: 0; r: 0; }
    }
    
    @keyframes power-pulse {
      0%, 100% { opacity: 0.6; r: 8; }
      50% { opacity: 1; r: 10; }
    }
    
    @keyframes float {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-3px); }
    }
    
    @keyframes blink {
      0%, 90%, 100% { opacity: 1; }
      95% { opacity: 0; }
    }
    
    @keyframes glow-pulse {
      0%, 100% { opacity: 0.1; }
      50% { opacity: 0.3; }
    }
    
    @keyframes score-up {
      0%, 80% { opacity: 0; transform: translateY(0); }
      85% { opacity: 1; transform: translateY(-5px); }
      100% { opacity: 0; transform: translateY(-15px); }
    }
    
    .devman-group {
      animation: devman-move 8s linear infinite;
    }
    
    .devman-body {
      animation: float 0.3s ease-in-out infinite;
    }
    
    .devman-eye {
      animation: blink 3s ease-in-out infinite;
    }
    
    .bug-group {
      animation: bug-chase 8s linear infinite;
      animation-delay: 0.8s;
    }
    
    .merge-group {
      animation: merge-chase 8s linear infinite;
      animation-delay: 1.2s;
    }
    
    .debt-group {
      animation: debt-chase 8s linear infinite;
      animation-delay: 1.6s;
    }
    
    .dot-1 { animation: dot-eaten 8s linear infinite; animation-delay: 1s; }
    .dot-2 { animation: dot-eaten 8s linear infinite; animation-delay: 2s; }
    .dot-3 { animation: dot-eaten 8s linear infinite; animation-delay: 3s; }
    .dot-4 { animation: dot-eaten 8s linear infinite; animation-delay: 4s; }
    .dot-5 { animation: dot-eaten 8s linear infinite; animation-delay: 5s; }
    .dot-6 { animation: dot-eaten 8s linear infinite; animation-delay: 6s; }
    
    .power-dot-anim {
      animation: power-pulse 1s ease-in-out infinite;
    }
    
    .maze-glow {
      animation: glow-pulse 2s ease-in-out infinite;
    }
    
    .score-popup {
      animation: score-up 8s linear infinite;
    }
    
    .enemy-float {
      animation: float 0.5s ease-in-out infinite;
    }
  </style>
  
  <!-- Background -->
  <rect class="bg" width="800" height="200"/>
  
  <!-- Maze Lines (decorative) -->
  <g class="maze-glow">
    <line x1="50" y1="50" x2="750" y2="50"/>
    <line x1="50" y1="150" x2="750" y2="150"/>
    <line x1="50" y1="50" x2="50" y2="150"/>
    <line x1="750" y1="50" x2="750" y2="150"/>
  </g>
  <g class="maze-line">
    <line x1="50" y1="50" x2="750" y2="50"/>
    <line x1="50" y1="150" x2="750" y2="150"/>
    <line x1="50" y1="50" x2="50" y2="150"/>
    <line x1="750" y1="50" x2="750" y2="150"/>
    <!-- Inner maze elements -->
    <line x1="100" y1="80" x2="200" y2="80"/>
    <line x1="100" y1="120" x2="200" y2="120"/>
    <line x1="600" y1="80" x2="700" y2="80"/>
    <line x1="600" y1="120" x2="700" y2="120"/>
  </g>
  
  <!-- Collectible Dots -->
  <circle class="dot dot-1" cx="180" cy="100" r="4"/>
  <circle class="dot dot-2" cx="250" cy="100" r="4"/>
  <circle class="dot dot-3" cx="320" cy="100" r="4"/>
  <circle class="dot dot-4" cx="390" cy="100" r="4"/>
  <circle class="dot dot-5" cx="460" cy="100" r="4"/>
  <circle class="dot dot-6" cx="530" cy="100" r="4"/>
  
  <!-- Power Pellet -->
  <circle class="power-dot power-dot-anim" cx="600" cy="100" r="8"/>
  
  <!-- Score Popup -->
  <text class="score-text score-popup" x="200" y="85">+10</text>
  
  <!-- DEV-MAN Character -->
  <g class="devman-group" transform="translate(80, 100)">
    <g class="devman-body">
      <!-- Body (Pac-Man shape) -->
      <circle class="devman" cx="0" cy="0" r="15"/>
      <!-- Mouth (triangle cut-out) -->
      <polygon points="0,0 18,-8 18,8" fill="#0a0a0f"/>
      <!-- Eye -->
      <circle class="devman-eye" cx="-3" cy="-7" r="3"/>
    </g>
  </g>
  
  <!-- Bug Enemy -->
  <g class="bug-group" transform="translate(30, 100)">
    <g class="enemy-float">
      <ellipse class="bug" cx="0" cy="0" rx="12" ry="10"/>
      <!-- Eyes -->
      <circle class="enemy-eye" cx="-4" cy="-2" r="4"/>
      <circle class="enemy-eye" cx="4" cy="-2" r="4"/>
      <circle class="enemy-pupil" cx="-3" cy="-2" r="2"/>
      <circle class="enemy-pupil" cx="5" cy="-2" r="2"/>
      <!-- Antenna -->
      <line x1="-5" y1="-10" x2="-8" y2="-15" stroke="#ff3366" stroke-width="2"/>
      <line x1="5" y1="-10" x2="8" y2="-15" stroke="#ff3366" stroke-width="2"/>
      <circle cx="-8" cy="-15" r="2" fill="#ff3366"/>
      <circle cx="8" cy="-15" r="2" fill="#ff3366"/>
    </g>
    <text x="-8" y="25" font-size="8" fill="#ff3366" font-family="monospace">BUG</text>
  </g>
  
  <!-- Merge Conflict Enemy -->
  <g class="merge-group" transform="translate(0, 100)">
    <g class="enemy-float" style="animation-delay: 0.2s;">
      <polygon class="merge-conflict" points="0,-12 12,8 -12,8"/>
      <!-- Eyes -->
      <circle class="enemy-eye" cx="-3" cy="0" r="3"/>
      <circle class="enemy-eye" cx="3" cy="0" r="3"/>
      <circle class="enemy-pupil" cx="-2" cy="0" r="1.5"/>
      <circle class="enemy-pupil" cx="4" cy="0" r="1.5"/>
    </g>
    <text x="-20" y="25" font-size="6" fill="#ff8800" font-family="monospace">CONFLICT</text>
  </g>
  
  <!-- Tech Debt Enemy -->
  <g class="debt-group" transform="translate(-30, 100)">
    <g class="enemy-float" style="animation-delay: 0.4s;">
      <rect class="tech-debt" x="-10" y="-10" width="20" height="20" rx="3"/>
      <!-- Skull face -->
      <circle class="enemy-eye" cx="-4" cy="-2" r="3"/>
      <circle class="enemy-eye" cx="4" cy="-2" r="3"/>
      <circle class="enemy-pupil" cx="-4" cy="-2" r="1.5"/>
      <circle class="enemy-pupil" cx="4" cy="-2" r="1.5"/>
      <rect fill="#0a0a0f" x="-5" y="4" width="10" height="3"/>
    </g>
    <text x="-10" y="25" font-size="7" fill="#aa55ff" font-family="monospace">DEBT</text>
  </g>
  
  <!-- Title -->
  <text class="title" x="400" y="30" text-anchor="middle">🎮 DEV-MAN: A Developer's Nightmare</text>
  <text class="subtitle" x="400" y="185" text-anchor="middle">Dodge bugs • Escape merge conflicts • Outrun tech debt • Power up with coffee!</text>
  
  <!-- Score Display -->
  <text class="score-text" x="70" y="30">SCORE: 1337</text>
  <text class="score-text" x="650" y="30">HIGH: 9999</text>
</svg>

